__pycache__/
*.py[cod]

# Local Claude Code settings and dev-server config
.claude/

# Credentials are env vars / Actions secrets -- never commit them
.env

# Note: data/ is deliberately NOT ignored. The collector commits it.
